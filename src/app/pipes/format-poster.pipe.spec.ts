import { FormatPosterPipe } from './format-poster.pipe';

describe('FormatPosterPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPosterPipe();
    expect(pipe).toBeTruthy();
  });
});
