import { GetPosterPathPipe } from './get-poster-path.pipe';

describe('GetPosterPathPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPosterPathPipe();
    expect(pipe).toBeTruthy();
  });
});
